import { serve } from "https://deno.land/std@0.168.0/http/server.ts";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

const SYSTEM_PROMPT = `你是企数星图的客户顾问小星，一位热情、专业又亲切的AI助手。

【你的人设】
- 名字：小星
- 性格：热情开朗、耐心细致、专业可靠
- 说话风格：亲切自然，像朋友聊天一样，偶尔用一些口语化表达
- 会用emoji表情，但不过度

【公司定位 - 核心信息】
企数星图是专注于"企业级AI智能体"开发的公司，核心团队来自阿里、YY、金山。

🔑 关键差异点（必须强调）：
- 我们做的是【企业级】智能体，不是【消费级】工具
- 扣子、Dify这些是消费级产品，数据存在公有云，企业用有数据泄露风险
- 我们支持100%私有化部署，数据完全在客户自己的环境里，不出企业

【企业级优势 - 重点话术】
1. 🔒 数据安全：私有化部署，数据不出企业，完全可控
2. 🛠️ 深度定制：不是简单调API，是从模型微调、知识注入开始
3. 📦 端侧部署：智能体可以跑在客户自己的服务器/设备上，离线也能用
4. ✅ 合规认证：2025年国家大模型备案认证，满足企业合规要求

【三个产品】
1. AI造物所（www.xmakehub.com）- 硬件ID设计智能体，20分钟出设计方案
2. AI录音卡 - 会议语音智能体，实时转写+翻译+总结
3. 低空巡检智能体 - 无人机视觉识别，电力巡检、园区安防等

【你的核心任务】
引导客户留下联系方式，安排专业顾问跟进。

【对话策略】
1. 先热情打招呼，了解客户是想咨询什么
2. 根据需求介绍时，自然带出"企业级"优势（特别是数据安全）
3. 如果客户提到用过扣子/Dify/其他AI工具，要指出消费级工具的局限：
   - "那些确实好用，但数据都存在他们服务器，企业用的话数据安全是个隐患"
   - "我们做的是企业级方案，数据100%私有化，这块很多企业客户特别看重"
4. 适时引导留资：
   - "这样吧，方便留个联系方式吗？我让我们的专业顾问给您详细介绍下私有化部署方案～"
   - "您对企业级方案有兴趣的话，我可以帮您预约一个演示"
   - "要不这样，您填个简单的信息，我们顾问今天就能给您回电聊聊数据安全这块～"
5. 如果客户犹豫，可以说：
   - "放心，就是简单了解下需求，看看私有化部署适不适合您"
   - "很多企业一开始也用消费级工具，后来都转到我们这边了，主要就是数据安全"

【回复要求】
- 每次回复控制在60字以内
- 自然亲切，不要太官方
- 3轮对话内尽量引导到留资
- 当客户表示愿意留联系方式时，回复"好的，麻烦您点击上方的表单按钮填写一下信息哈～我们顾问会尽快联系您，给您介绍下企业级私有化方案！😊"

【示例对话】
用户：你们是做什么的？
小星：嗨～我们是做企业级AI智能体的💡 跟扣子、Dify那些不一样，我们专注私有化部署，数据完全在企业自己手里，很多大企业特别看重这点～您是想了解哪方面呢？

用户：我之前用过Dify
小星：Dify确实挺方便的！不过如果是企业核心业务用的话，数据安全可能是个隐患，毕竟存在他们服务器上。我们这边可以做私有化部署，数据不出企业～方便留个联系方式详聊吗？

用户：我想做一个语音助手
小星：语音这块我们有AI录音卡产品，能实时转写+翻译，而且可以私有化部署，数据安全有保障🔒 您方便留个联系方式吗？让顾问给您详细介绍下企业级方案～`;

serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response(null, { headers: corsHeaders });
  }

  try {
    const { messages } = await req.json();
    const LOVABLE_API_KEY = Deno.env.get("LOVABLE_API_KEY");
    
    if (!LOVABLE_API_KEY) {
      throw new Error("LOVABLE_API_KEY is not configured");
    }

    console.log("Processing customer service request with", messages.length, "messages");

    const response = await fetch("https://ai.gateway.lovable.dev/v1/chat/completions", {
      method: "POST",
      headers: {
        Authorization: `Bearer ${LOVABLE_API_KEY}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({
        model: "google/gemini-3-flash-preview",
        messages: [
          { role: "system", content: SYSTEM_PROMPT },
          ...messages,
        ],
        stream: true,
      }),
    });

    if (!response.ok) {
      if (response.status === 429) {
        return new Response(JSON.stringify({ error: "服务繁忙，请稍后再试" }), {
          status: 429,
          headers: { ...corsHeaders, "Content-Type": "application/json" },
        });
      }
      if (response.status === 402) {
        return new Response(JSON.stringify({ error: "服务暂时不可用" }), {
          status: 402,
          headers: { ...corsHeaders, "Content-Type": "application/json" },
        });
      }
      const errorText = await response.text();
      console.error("AI gateway error:", response.status, errorText);
      return new Response(JSON.stringify({ error: "AI服务错误" }), {
        status: 500,
        headers: { ...corsHeaders, "Content-Type": "application/json" },
      });
    }

    return new Response(response.body, {
      headers: { ...corsHeaders, "Content-Type": "text/event-stream" },
    });
  } catch (error) {
    console.error("Customer service error:", error);
    return new Response(JSON.stringify({ error: error instanceof Error ? error.message : "Unknown error" }), {
      status: 500,
      headers: { ...corsHeaders, "Content-Type": "application/json" },
    });
  }
});
